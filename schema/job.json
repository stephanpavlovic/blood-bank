{
  "type": "object",
  "title": "job",
  "description": "A job offering",
  "name": "job",
  "properties": {
    "slug": {
      "description": "the job's slug.",
      "required": true,
      "type": "string"
    },
    "title": {  
      "description": "the job's title.",
      "required": true,
      "type": "string"
    },
    "description": {
      "description": "description of the job.",
      "required": true,
      "type": "string"
    }
  }
}
